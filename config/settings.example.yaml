# 配置模板：复制为 config/settings.yaml 后按环境填写
# （settings.yaml 含本机数据库密码，已被 .gitignore 排除，不入仓库）
#
# base_url: http://localhost:8080
# accounts:
#   admin:
#     username: admin
#     password: Qa123456          # 被测系统登录账号（Compose 环境由 02_test_password.sql 重置）
# database:
#   host: "localhost"
#   port: 3306
#   user: "root"
#   password: "<本机 MySQL root 密码>"
#   database: "mall_tiny"
# redis:
#   host: "localhost"
#   port: 6379
#   db: 0
#   decode_responses: True
